---
import { Navbar } from 'react-bootstrap';
import { theme } from '../utils/theme';
---

<header>
    <Navbar>
        <Navbar.Brand href="/">
            <img src={`${theme.id}/logo.svg`} alt={theme.name} />
        </Navbar.Brand>
    </Navbar>
</header>
