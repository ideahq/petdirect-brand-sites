---
import { Navbar } from 'react-bootstrap';
import { theme } from '../utils/theme';
---

<header>
    <Navbar className="p-3">
        <Navbar.Brand href="/">
            <img class="logo" src={`${theme.id}/logo.svg`} alt={theme.name} />
        </Navbar.Brand>
    </Navbar>
</header>
