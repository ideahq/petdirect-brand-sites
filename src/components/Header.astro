---
import Navigation from './Navigation';
import { theme } from '../utils/theme';

const links = [
    {
        name: "About",
        url: "#"
    },
    {
        name: "Shop",
        url: "#"
    }
]
---

<header>
    <Navigation
        client:visible
        name={theme.name}
        id={theme.id}
        links={links}
    />
</header>
