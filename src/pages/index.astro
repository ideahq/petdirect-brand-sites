---
import Layout from '../layouts/Layout.astro';
import { Stack } from 'react-bootstrap';
import Hero from '../components/Hero.astro';
import Products from '../components/Products.astro';
import PeggysPantryInfo from '../components/peggys-pantry/PeggysPantryInfo.astro';
import WildPawInfo from '../components/wild-paw/WildPawInfo.astro';
import CalibreInfo from '../components/calibre/CalibreInfo.astro';
import PeggysPantryBenefits from '../components/peggys-pantry/PeggysPantryBenefits.astro';
import Gallery from '../components/Gallery.astro';
import { theme } from '../utils/theme';
import '../styles/wild-paw/styles.scss';
---

<Layout>
    <Stack>
        <Hero />
        <Products />
        {theme.id === 'peggys-pantry' && (
            <PeggysPantryInfo />
            <PeggysPantryBenefits />
        )}
        {theme.id === 'wild-paw' && (
            <WildPawInfo />
        )}
        {theme.id === 'calibre' && (
            <CalibreInfo />
        )}
        <Gallery />
    </Stack>
</Layout>
